[package]
name = "azurite_codegen"
version = "0.1.0"
edition = "2021"

[dependencies]
azurite_parser = { path = "../azurite_parser" }
inkwell = { version = "0.4", optional = true }

[features]
default = []
llvm = ["dep:inkwell"]
