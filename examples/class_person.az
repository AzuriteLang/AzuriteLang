class Person {
    name: string
    age: int

    func new(name: string, age: int) {
        // constructor
    }

    func greet(self) {
        println(self.name)
        print(self.age)
    }
}

func main() {
    let p = Person.new("Alice", 30)
    p.greet()
}
